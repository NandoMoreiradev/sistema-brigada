[build]
builder = "NIXPACKS"
buildCommand = "npx prisma generate && npx nest build -b swc"

[deploy]
preDeployCommand = "npm run prisma:seed"
startCommand = "npm run start:prod"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
